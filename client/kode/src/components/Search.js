import React from "react";

const Search = () => {
  return (
    <input
      type="text"
      placeholder="What are you looking for?"
      className="search"
    />
  );
};

export default Search;
